import React, { FC } from "react";
import Router from "./Router/Router";
import Navbar from "./Components/Navbar";

const App: FC = () => {
  return (
    <div>
      <Router />
    </div>
  );
};

export default App;
